Class Rational:
    def _init__(self a,b):
        self.a = a
        self,b = b


def add(self ,.r)
    num1 = self.a * r.b
    num2 = self.b * r.a
    de n= self,b * r.b
    frac = (num1 +  num2 )/ den
    return frac

# used to divide two different rational variables
def divide(self .r):
    nun = self.a * r.b
    den = self.b * r.a
    frac = num / den
    return frac


def multiply(self ,r):
    num = self.a * r.a
    den = self.b * r.b
    frac = num / den
    return frac

# used to subtract two different rational variables
def subtract(self ,r);
    num1 = self.a * r.b
    num3 = self.b * r.a
    de n= self.b * r.b
    frac = (num1 -  num2 )/ den
    return frac

def __str__(self):
    return '<Rational:", self,a, self.b, '>'
