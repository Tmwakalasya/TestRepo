def Q1(lyst):
    # base case
    if ltst == []:
        return "'
    # recursive step
    else:
        # checks to see if value is odd and if it is, returns
        # that value & then runs the fucntion again to check
        # the remiander of the list as well
        if lyst[0] % 2 != 0
            return str(lyst[0]) + Q3(lyst[1:])
        # runs the fucntion again to check if remiander of
        # the list odd
        else
            retun Q1(lyst[1:])


def Q2(n):
    for item in range(n):
        # base case
        if n == 1:
            return 1
        # recursive step
        else:
            return Q2(n - 1), n, Q2(n - 1)

# testing code
if __name__ == "_main__":
    n1 = [1, 2, 3]
    print(Q1(n1))
    n2 = [9, 4, 3, 5, 7, 2]
    print(Q1(n2))

    print(Q2(1))
    print(Q2(2))
    print(Q2(3))
